# Heimdall - Configuración
# Solo necesario si usas --method sql o decrypt

DB_HOST=localhost
DB_PORT=3306
DB_USER=admin
# DB_PASSWORD=    <-- se auto-detecta desde /etc/psa/.psa.shadow si está vacío

# Tiempo entre llamadas a HIBP
# HIBP_RATE_LIMIT=1.5
